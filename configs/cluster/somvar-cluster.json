{
  "__default__": {
    "t": "00:45",
    "mem": "1000",
    "nodes": 1,
    "partition": "debug",
    "threads": "{threads}",
    "name": "{rule}:{wildcards.sample}_{wildcards.type}.{wildcards.chrom}"
  },
  "mpileup": {
    "t": "00:45",
    "mem": 400
  },
  "varscan": {
    "t": "00:30",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}.{wildcards.chrom}"
  },
  "varscan2table": {
    "t": "00:05",
    "mem": 100,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}.{wildcards.chrom}"
  },
  "merge_varscan_tables": {
    "t": "00:10",
    "mem": 1000,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "annovar": {
    "t": "00:45",
    "mem": 100,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor_normal}"
  },
  "fisher_strand": {
    "t": "00:05",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor_normal}"
  },
  "anno2EB": {
    "t": "00:45",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}.{wildcards.chrom}"
  },
  "anno2EBcache": {
    "t": "00:30",
    "mem": 250,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}.{wildcards.chrom}"
  },
  "mergeEBscore": {
    "t": "00:05",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "merge_annos": {
    "t": "00:05",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor_normal}"
  },
  "edit_table": {
    "t": "00:05",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor_normal}"
  },
  "filter1": {
    "t": "00:05",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "primer3": {
    "t": "00:30",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "filter_bam": {
    "t": "00:45",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}_{wildcards.type}"
  },
  "filter_pileup": {
    "t": "00:30",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}_{wildcards.type}.{wildcards.chrom}"
  },
  "detect_HDR": {
    "t": "00:30",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}_{wildcards.type}.{wildcards.chrom}"
  },
  "merge_HDR": {
    "t": "00:05",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "combine_filter1": {
    "t": "00:15",
    "mem": 1000,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "filter2": {
    "t": "00:15",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  },
  "IGVnav": {
    "t": "00:05",
    "mem": 100,
    "name": "{rule}:{wildcards.sample}_{wildcards.tumor}-{wildcards.normal}"
  }
}